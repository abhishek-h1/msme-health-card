# Copy this to backend/.env and fill in real values.

# Anthropic API key used by the /agents modules (risk/recommendation/narrative
# pipeline). ANTHROPIC_API_KEY also works -- either is read.
LLM_API_KEY=your_api_key_here

# SQLite connection string used by SQLAlchemy.
DATABASE_URL=sqlite:///./msme_health.db
